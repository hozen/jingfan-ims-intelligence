# 工业雷达补数提示

请针对 `customer/industrial-leads/pipeline-manifest.json` 中的每个 Lead ID 补齐以下信息。只写已核实事实，不要根据公司名称或项目名称推测。

## 必须返回

1. **Stage 1 原始发现**：Lead ID、公司、项目、发现日期、原始来源链接、原文摘录，并确认是否与当前记录一致。
2. **Stage 2 判断**：只能填写 `YES`（可以跟进）、`REVIEW`（需要人工复核）或 `NO`（不跟进）；同时写出事实依据。
3. **Stage 3 输入条件**：只有 Stage 2 为 `YES` 的 Lead 才提交给 Water Clay。
4. **Stage 3 补充事实**：项目当前进度、业主/设计/施工/供应商、采购负责人或技术负责人姓名、公开联系方式（如确实找到）。
5. 每条事实都要附来源链接和原文摘录；找不到的字段明确写“未找到”。

## 当前已知缺口

- 2026-09-10 的 3 条记录已有 Stage 2 `YES` 和 Water Clay 结果，但没有找到对应的 Stage 1 原始记录，需要补链并核对。
- 2026-08-21 的 14 条记录已有 Stage 1 发现事实，但尚未找到对应的 Stage 2 判断，因此目前不能说它们值得跟进，也不能送入 Water Clay。

## 可直接复制的任务

请补齐以上缺口，并按每个 Lead ID 单独返回 JSON。不要覆盖已有事实；新增字段请附 `source_url`、`quote`、`published_at`。如果没有证据，请返回空值并说明“未找到”。
